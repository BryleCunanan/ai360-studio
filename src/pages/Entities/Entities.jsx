import { Outlet } from "react-router-dom";

const Entities = () => {
  return (
    <h1
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "50%",
      }}
    >
      Coming Soon...
    </h1>
  );
  // <Outlet />;
};

export default Entities;
